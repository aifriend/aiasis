# Audio capture
sounddevice>=0.5.0
numpy>=1.26.0

# Voice Activity Detection
silero-vad>=5.1

# Speech-to-Text (raw WebSocket to Deepgram v2 Flux API)
websockets>=13.0

# LLM (pick one or both)
openai>=1.50.0
anthropic>=0.39.0

# Text-to-Speech
edge-tts>=6.1.0

# Audio playback for TTS output
pydub>=0.25.1
audioop-lts>=0.2.1      # audioop shim for Python 3.13+ (pydub dependency)
certifi>=2024.0         # macOS SSL cert fix for Python.org builds

# .env file loading
python-dotenv>=1.0.0

# Dashboard
gradio>=5.0.0
